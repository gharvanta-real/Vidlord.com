import 'package:video_player/video_player.dart';

VideoPlayerController getController(String filePath, String? networkUrl) {
  throw UnsupportedError('Cannot create controller without platform implementation');
}
